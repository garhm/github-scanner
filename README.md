To run Apollo GraphQL server
cd server
npm run dev

To run client
cd client
npm start

Open http://localhost:3000
Type in Github personal access token
Repository names are preselected to scan different repos in different account use another personal access token
